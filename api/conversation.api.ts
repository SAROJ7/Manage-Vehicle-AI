export const createMessage = async ({
  conversationId,
  data,
}: {
  conversationId: string;
  data: { userId: string; message: { role: string; content: string } };
}) => {
  // Your implementation
};
